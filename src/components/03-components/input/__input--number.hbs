<input
  type="number"
  {{#if input.className}}class="{{ input.className }}"{{/if}}
  {{#if input.id}}id="{{ input.id }}"{{/if}}
  {{#if input.name}}name="{{ input.name }}"{{/if}}
  {{#if input.min}}min="{{ input.min }}"{{/if}}
  {{#if input.max}}max="{{ input.max }}"{{/if}}
  {{#if input.placeholder}}placeholder="{{ input.placeholder }}"{{/if}}
  {{#if input.required}}required{{/if}}>
