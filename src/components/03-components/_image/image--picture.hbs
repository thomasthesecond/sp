<picture>
  {{#each image.srcset}}
    <source
      srcset="{{ path this.src }}"
      {{#if this.media}}media="{{ this.media }}"{{/if}}>
  {{/each}}

  <img{{#if image.class}} class="{{ image.class }}"{{/if}}
    src="{{ path image.src }}"
    alt="{{ image.title }}">
</picture>
