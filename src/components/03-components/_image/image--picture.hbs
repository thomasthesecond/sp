<picture>
  {{#each image.srcset}}
    <source
      srcset="{{ this.src }}"
      {{#if this.media}}media="{{ this.media }}"{{/if}}>
  {{/each}}

  <img{{#if image.class}} class="{{ image.class }}"{{/if}}
    src="{{ image.src }}"
    alt="{{ image.title }}">
</picture>
