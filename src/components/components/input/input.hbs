<input
  {{#if type}}type="{{ type }}"{{/if}}
  {{#unless type}}type="text"{{/unless}}
  {{#if className}}class="{{ className }}"{{/if}}
  {{#if id}}id="{{ id }}"{{/if}}
  {{#if name}}name="{{ name }}"{{/if}}
  {{#if placeholder}}placeholder="{{ placeholder }}"{{/if}}
  {{#if required}}required{{/if}}>
